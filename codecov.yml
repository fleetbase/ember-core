coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%

flags:
  ember-core:
    paths:
      - addon/
    carryforward: false

comment:
  layout: 'condensed_header, diff, flags'
  behavior: default
  require_changes: false
