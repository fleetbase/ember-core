import corslite from 'dummy/utils/corslite';
import { module, test } from 'qunit';

module('Unit | Utility | corslite', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = corslite();
        assert.ok(result);
    });
});
