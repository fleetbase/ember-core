import serializeNormalizePolymorphicType from 'dummy/utils/serialize/normalize-polymorphic-type';
import { module, test } from 'qunit';

module('Unit | Utility | serialize/normalize-polymorphic-type', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = serializeNormalizePolymorphicType();
        assert.ok(result);
    });
});
