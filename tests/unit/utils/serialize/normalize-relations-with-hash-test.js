import serializeNormalizeRelationsWithHash from 'dummy/utils/serialize/normalize-relations-with-hash';
import { module, test } from 'qunit';

module('Unit | Utility | serialize/normalize-relations-with-hash', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = serializeNormalizeRelationsWithHash();
        assert.ok(result);
    });
});
