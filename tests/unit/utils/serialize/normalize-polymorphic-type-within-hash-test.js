import serializeNormalizePolymorphicTypeWithinHash from 'dummy/utils/serialize/normalize-polymorphic-type-within-hash';
import { module, test } from 'qunit';

module('Unit | Utility | serialize/normalize-polymorphic-type-within-hash', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = serializeNormalizePolymorphicTypeWithinHash();
        assert.ok(result);
    });
});
