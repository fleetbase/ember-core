import isModel from 'dummy/utils/is-model';
import { module, test } from 'qunit';

module('Unit | Utility | is-model', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = isModel();
        assert.ok(result);
    });
});
