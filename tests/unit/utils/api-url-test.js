import apiUrl from 'dummy/utils/api-url';
import { module, test } from 'qunit';

module('Unit | Utility | api-url', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = apiUrl();
        assert.ok(result);
    });
});
