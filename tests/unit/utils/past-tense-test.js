import pastTense from 'dummy/utils/past-tense';
import { module, test } from 'qunit';

module('Unit | Utility | past-tense', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = pastTense();
        assert.ok(result);
    });
});
