import extractLongitude from 'dummy/utils/extract-longitude';
import { module, test } from 'qunit';

module('Unit | Utility | extract-longitude', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = extractLongitude();
        assert.ok(result);
    });
});
