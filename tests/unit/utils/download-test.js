import download from 'dummy/utils/download';
import { module, test } from 'qunit';

module('Unit | Utility | download', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = download();
        assert.ok(result);
    });
});
