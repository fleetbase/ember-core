import loadExtensions from 'dummy/utils/load-extensions';
import { module, test } from 'qunit';

module('Unit | Utility | load-extensions', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = loadExtensions();
        assert.ok(result);
    });
});
